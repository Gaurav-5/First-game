#include<stdio.h>

int main(){
    int x,y;
    printf("Enter the length of rectangle \n",x);
    scanf("%d",&x);
    printf("Enter the breath of rectangle \n",y);
    scanf("%d",&y);
    
    // printf("area of rectrangle of is %d ",x*y);

    return 0;
}